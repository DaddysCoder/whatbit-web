# Copy to .env.local and fill in real values. See README.md for full details
# on each variable and how the app degrades when they're absent.

# --- Contact form + AI Blueprint early-access waitlist (marketing only) ---
RESEND_API_KEY=
CONTACT_FROM_EMAIL=website@whatbit.dev

# --- AI Blueprint standalone app (marketing link only) ---
# Future origin of the standalone AI Blueprint app (DaddysCoder/blue-print-ai-app).
# Empty until the owner approves the domain; marketing-only, no secrets here.
NEXT_PUBLIC_AI_BLUEPRINT_APP_URL=
